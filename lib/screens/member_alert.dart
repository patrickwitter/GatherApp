import 'package:flutter/material.dart';

class AlertMember extends StatelessWidget {
  const AlertMember({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(" Alert Member"),
    );
  }
}
