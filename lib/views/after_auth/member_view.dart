import 'package:flutter/material.dart';

class MemeberView extends StatelessWidget {
  const MemeberView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("MemberView"),
      ),
    );
  }
}
